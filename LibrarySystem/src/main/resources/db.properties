# Database Configuration — Library Management System
# -------------------------------------------------------
# IMPORTANT: Update the values below to match your MySQL
# installation before running the application.
# Do NOT commit real credentials to source control.
# -------------------------------------------------------

db.url=jdbc:mysql://localhost:3306/library_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username=root
db.password=your_password_here

# Connection pool settings
db.pool.maxConnections=10
db.pool.connectionTimeout=30000
